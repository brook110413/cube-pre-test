/** What is virtual DOM and what purpose does it aim to solve?? **/

/* 作答區 */
/*
virtual DOM 是一個虛擬的 DOM tree，用於模擬真實的 DOM tree。因為操作真實的 DOM 會連動瀏覽器的渲染引擎重新繪製畫面，而這是一件效能成本很大的事，因此借由 virtual DOM 來模擬真實的 DOM，而不是直接來操作真實的 DOM，來解決瀏覽器渲染 DOM 的性能問題。
*/
